# 隐私说明

WorkMate Pro 的默认原则是：能在本机完成的能力不离开本机。

本页新增的显式授权、恢复与统计行为说明适用于当前 main 源码构建；现有 v1.26.0 Release 发布包尚未包含本轮改进，将随下一次单独发布交付。使用现有发布包时，请主动检查并关闭不需要的天气/环境和 Outlook 能力。

## 本机读取与保存

- 为判断工作状态，程序读取当前前台进程的**进程名**并映射为“开发工具、浏览器、沟通协作”等类别；不读取窗口标题或文档正文。
- Raw Input 在后台接收键盘、滚轮、点击和移动事件，只在内存中计算频率与空闲时间，不保存键码、输入文字或鼠标轨迹。系统麦克风/摄像头占用状态与音频播放电平用于判断会议和观看状态，不录制音视频内容。
- Outlook 会议雷达默认关闭；用户开启后通过本机 Outlook 读取未来两天及仍在进行的会议，包括主题、起止时间、地点和必需/可选参会人。结果只驻留内存，关闭即清除缓存；WorkMate 不上传会议信息。Outlook 自身的账户同步由 Outlook 管理。
- 备忘、成长值、分类统计、文件暂存路径、设置和城市名保存在 `%APPDATA%\WorkMatePro\data.json`。
- 自定义宠物的参考照片、提示词与生成结果保存在 `%APPDATA%\WorkMatePro\CustomPets\`。
- 剪贴板 OCR 会先把图片副本写入 `%APPDATA%\WorkMatePro\OCR\Inbox\`，识别文本写入 `%APPDATA%\WorkMatePro\OCR\` 并尝试复制回剪贴板。
- “叼住剪贴板文字 / 链接”只有在用户点击后才读取剪贴板；“识别剪贴板图片”同样需要用户主动操作。
- WorkMate 不再包含任何自动截屏能力：没有滚动/翻页截图，也不会在后台抓取屏幕像素。

这些数据没有应用层加密；能访问当前 Windows 用户文件的程序也可能读取它们。敏感工作环境应按需要关闭统计、定期清理 OCR 输出，并保护 Windows 账户。

## 网络请求

WorkMate 没有账号系统、广告、遥测或崩溃上报。更新机制默认只扫描本机 `%LOCALAPPDATA%\WorkMatePro\Updates\Inbox\`、程序所在目录和下载目录，不会在后台联网。只有用户点击“手动检查 GitHub”时，才会通过 HTTPS 获取本仓库最新 Release 中很小的 `update-catalog.json` 与签名文件；程序不会自动下载或安装 EXE。

新安装的天气、户外、早报、环境共感和 Outlook 读取均默认关闭，城市留空。本地备忘、桌宠与统计无需联网或账号。升级保留已有城市及各项提醒配置，但新增的“允许天气联网”授权缺失时默认关闭，需在设置中明确开启；不会把旧默认城市当成新用户已选择的城市。

只有开启“设置 → 允许天气联网”后，天气/环境能力才可访问以下 Open-Meteo HTTPS 服务（手动查询和自动刷新遵守同一授权）：

- `geocoding-api.open-meteo.com`
- `api.open-meteo.com`
- `air-quality-api.open-meteo.com`

请求包含用户填写的城市名以及由地理编码得到的经纬度。和任何网络请求一样，服务端还会看到连接所必需的 IP/时间等元数据。关闭“允许天气联网”会阻止后续天气请求，丢弃在途结果并清除提醒；已经发送的请求无法撤回。手动本地早报仍可使用，不会自动开启天气授权。此开关只管天气服务，用户主动点击“手动检查 GitHub”仍是单独的一次联网操作。

“暂停时间统计”停止应用类别、小时、心流与中断记录，并丢弃未完成统计片段；实时免内容信号仍用于桌宠行为。“清空今日统计”也清除心流与中断，保留已经发放的成长奖励账本，避免重复领奖。

WorkMate 的自定义宠物功能只建立本地工作目录，不会代替用户把照片发送给图片模型。若用户自行把照片交给第三方生成服务，其隐私边界由该服务决定。

## 删除数据

1. 在 WorkMate 中关闭“开机启动”，或删除注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 下的 `WorkMatePro` 值。
2. 退出 WorkMate。
3. 删除 `%APPDATA%\WorkMatePro\` 与 `%LOCALAPPDATA%\WorkMatePro\`。
4. 删除下载的 EXE；如果曾用旧版本的滚动截图，再删除当时自己选择的输出目录。

以上操作会永久删除相应本地数据，请先备份需要保留的备忘或自定义宠物素材。
