# WorkMate Pro v1.24.0

这是首个公开 GitHub Release：一个无需安装的 Windows 10/11 便携桌宠 EXE。

重点更新：

- 今日一件事、每日奖励与可逆撤销；
- 天气/空气质量/紫外线环境共感；
- 低能量 / 稳稳来 / 精力足三档陪伴策略；
- 1–3 张照片的本地自定义宠物工作流；
- 智能视觉对齐滚动截图，同时支持固定 `@` 推进比例；
- Windows 本地 OCR；
- 离线优先的 RSA 签名增量更新：本地目录检测、手动导入、MSDelta、精确基线哈希、独立替换与健康检查回滚；
- 构建可复现性、内嵌工具哈希、超时与异常恢复加固。

下载 `WorkMate-1.24.0.exe` 后请用同一 Release 的 `SHA256SUMS.txt` 校验。程序未做 Authenticode 商业签名，SmartScreen 可能提醒。

这是第一版带内置更新器的公开基线：从旧版迁移到 v1.24.0 仍需手动替换完整 EXE；从 v1.24.0 升级到后续版本时，Release 将按旧版 EXE 的精确 SHA-256 提供小型差分包，并保留全量包兜底。本 Release 同时附带签名全量更新包和签名目录，用于建立并验证后续发布流程。

发布前门禁：300 帧素材检查；12 次串行 + 6 次并行完整自测；确定性压力、损坏数据恢复和事件风暴；截图算法 15 个单测及多轮真实桌面 smoke；更新签名篡改拒绝、40 MB EXE 差分、自替换健康检查与故障回滚，全部通过。

隐私边界与已知限制见仓库中的 `PRIVACY.md` 和 `CHANGELOG.md`。
