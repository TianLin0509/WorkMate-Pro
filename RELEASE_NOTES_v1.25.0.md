# WorkMate Pro v1.25.0

本版把“用自己的宠物做桌宠”从文件夹说明升级为 App 内完整四步向导：**选照片 → 复制生成指引 → 逐张映射四个姿态 → 校验并启用**。

主要变化：

- 草稿自动留在本机，可关闭 App 后继续；设置页会显示最近项目和已就绪数量；
- 生成提示词直接在 App 内预览和复制，同时保留参考图、项目目录和离线指南入口；
- `idle / typing / happy / sleep` 可逐张选择或拖放，原始 PNG 文件名可以不同；
- 透明背景、画布、主体中心、尺度与脚底基线未通过时，就地显示具体原因和修复建议；
- 导入使用暂存目录、原子交换和有界备份，失败不会破坏上一次可用资源；
- 修复启动时重复扫描自定义资源及后台清理 UI 图片缓存的竞态。

从官方 v1.24.1 升级时，下载同一 Release 中的 `WorkMate-delta-1.24.1-to-1.25.0.workmate-update.zip`，拖到桌宠身上并确认一次即可。若当前 EXE 不是官方 v1.24.1 的精确哈希，请改用 `WorkMate-full-1.25.0.workmate-update.zip` 或完整 EXE。

图片生成仍由用户自行选择外部工具。WorkMate 不上传照片、不保存第三方 API 密钥；参考照片、提示词、草稿与最终资源均保存在本机 `CustomPets` 目录。

同一 Release 附带自包含的 `WorkMate-v1.25.0-deep-audit.html`，可离线查看四步真实界面、审查修复项、压力数据和更新 E2E 证据。

发布 EXE 仍未做 Authenticode 商业签名，SmartScreen 可能在首次运行时提醒。请只从本仓库 Release 下载，并核对 `SHA256SUMS.txt`。
