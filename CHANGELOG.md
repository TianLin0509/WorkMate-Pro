# Changelog

## v1.26.0 — 2026-08-14

### 移除

- 完整移除智能滚动截图（自动翻页截图）：实现问题过多，不再维护。
  - 删除 `tools/AutoPageCapture/` 全部源码、测试、桌面 smoke、PyInstaller 构建脚本与启动器。
  - 删除能力中心的“开始智能滚动截图”卡片，桌宠菜单副标题改为“OCR · 天气 · 离线更新”。
  - 删除 `App.OpenScrollCapture()` 及 `scrollCaptureActive` 避让状态；截图期间的桌宠隐藏/恢复分支一并移除。
  - 删除 `EmbeddedToolManager` 的 `ScrollCaptureResource` / `ExtractScrollCapture` / `LaunchScrollCapture` 与 `ScrollCaptureSha256` 生成；EXE 内嵌能力工具由 2 个降为 1 个（仅 OCR 脚本）。
  - 构建脚本移除 `-RebuildTools` 开关；构建与 CI 不再需要 Python 3.12 及其锁定依赖。
  - 移除 `third_party/licenses/` 与 THIRD_PARTY_NOTICES 索引表：Python/Tk/Pillow/PyInstaller 仅由该工具引入，现已无第三方可再分发组件。

## v1.25.0 — 2026-08-10

### 新增

- 自定义宠物改为 App 内四步向导：身份与照片、生成指引、四张图片、校验并启用；每一步都有清晰进度、返回路径和固定主操作。
- 支持列出并继续本地草稿/已就绪项目；关闭 WorkMate 后无需重来。
- 四个姿态可逐张选择或直接拖到对应行，原文件名不受限制；WorkMate 会映射为 `idle / typing / happy / sleep` 契约。
- App 内展示完整提示词、参考图入口、离线指南、四姿态预览、透明比例/主体比例/脚底指标与可执行修复建议。

### 修复与加固

- 参考照片不再只看扩展名：增加容器完整性、实际解码、像素尺寸、文件大小和 1–3 张数量门禁。
- 项目创建、生成图准备和最终导入均采用暂存目录与原子交换；失败保留原项目，生成图备份最多 2 份、已启用资源备份最多 3 份。
- 在交换生成目录前验证项目归属和 manifest 身份，拒绝越界项目、ID 篡改及目录联接。
- 自定义宠物导入后只增量更新当前定义，不再全量重扫全部项目；同时移除启动阶段一次重复扫描。
- 图片缓存失效移回 UI 线程，避免后台导入与桌宠渲染并发访问缓存。
- 离线 `workflow.html` 与 App 内四步流程同步，不再引用已删除的旧按钮或强制原文件名。

### 发布门禁

- 自定义宠物压力：24 个并发项目、12 次完整导入、500 次并发读取和重复导入备份上限验证通过。
- 真实 WPF 四步 UI 在 225% DPI 下逐页捕获为 2205×1575，稳定 AutomationId、非空画面和运行时错误日志门禁全部通过。
- 真实 UI 点击完成 1→2→3→4、最终原子导入并选中自定义宠物；故意损坏 `sleep.png` 后内联修复提示通过辅助功能树断言。
- 完整自测覆盖截断照片、manifest 篡改、任意文件名映射、事务导入、运行时加载、损坏资源跳过和备份上限；12 次串行 + 6 次并行隔离运行全部通过。
- 全局压力：25 万次状态引擎、10 万次提醒队列、500 次原子保存、2 万次天气解析、3 万次精灵读取，0 失败；40 次运行时事件风暴未生成错误日志。
- 智能截图 15 项算法测试通过；真实 Windows 截屏/滚轮连续 5 轮均输出 5 页、总高度 5048 px、0 次保守回退。
- v1.24.1 → v1.25.0 签名差分包为 497,006 字节（目标 EXE 的 1.2%）；差分、全量兜底及拖放协调器均完成隔离自替换、健康检查和旧版备份。

### 已知边界

- 图片生成仍由用户自行选择外部工具；WorkMate 不上传照片、不保存第三方 API 密钥。
- 自定义宠物使用四张状态图，复杂逐帧表演会安全回退到轻量程序动效。
- 发布 EXE 未做 Authenticode 商业签名，SmartScreen 可能提醒。

## v1.24.1 — 2026-08-09

### 新增

- 更新专用拖放：将单个 `*.workmate-update.zip` 拖到桌宠身上，松开后自动验签和暂存，只确认一次即可退出、更新并自动重启。
- 拖放时显示“松开即可更新”状态；验签中、拒绝、取消和更新器启动均通过高优先级桌宠提示反馈，安静模式也不会吞掉用户主动更新结果。

### 加固

- 更新拖放与已回退的普通文件中转完全隔离；普通 ZIP、源码 ZIP、文本、目录及多个文件不会触发安装。
- 能力中心与桌宠拖放共用单一安装协调器，避免两套确认、清理和异常处理逻辑漂移。
- 同一时间只允许验证一个更新包；用户取消或更新器启动失败时立即清理本次隔离暂存目录。
- 串行化共享更新清单解析，避免启动后的本地扫描与手动拖入同时发生时争用非线程安全解析器。
- 增加隔离测试专用的拖放安装 E2E 入口，生产环境无法跳过人工确认。

## v1.24.0 — 2026-08-09

### 新增

- 今日一件事：每日唯一优先项、完成奖励和完整撤销账本。
- 环境共感：天气、空气质量、紫外线与降雨状态进入桌宠状态提示。
- 能量模式：低能量 / 稳稳来 / 精力足，联动动作与提醒策略。
- 自定义宠物工坊：1–3 张本地参考照片、提示词工作流、四姿态质量门禁、事务式导入与备份。
- 智能滚动截图：滚轮响应校准、视觉重叠对齐、智能/固定比例模式、流式去重分页。
- Windows 本地 OCR：支持剪贴板和常见图片文件。
- 离线增量更新：签名目录、本地收件箱与手动导入；Windows MSDelta 差分、全量兜底、精确基线校验、独立替换和启动失败回滚。

### 修复与加固

- 消除构建脚本中的本机绝对路径，源码可在任意目录复建。
- 把滚动截图组件源码纳入仓库；构建时动态生成并验证内嵌工具 SHA-256。
- OCR 子进程增加 60 秒超时，避免异常环境下无限等待。
- 截图目标激活失败时恢复 UI 并返回错误，不再停留在隐藏状态。
- 修复虚拟环境 Python 启动器下桌面 smoke 无法识别子窗口的问题。
- 修复自测对旧二进制字节数和相对测试路径的硬编码。
- 修复周期性列表中滚轮批次丢失一个刻度时，运动先验压过更强像素证据而造成重复行的问题；改为逐增量对齐、多候选全高度复核。

### 发布门禁

- 300 张动画帧质量检查：0 个问题。
- WorkMate 确定性压力：25 万次状态引擎、10 万次提醒队列、500 次原子保存、2 万次天气解析、3 万次精灵读取。
- 完整自测：12 次串行 + 6 次并行隔离运行，全部通过；损坏 JSON 恢复与 40 次事件风暴通过。
- 智能截图：15 个单元测试通过，覆盖周期行、丢失滚轮刻度与直接粘贴 `@` 比例；真实桌面 smoke 连续 5 轮均输出 5 页、总高度 5048 px、0 次保守回退。
- 更新系统：RSA 真签名通过、篡改包在替换前拒绝；40.4 MB EXE 差分成功；隔离目录内完成自替换健康检查，并通过故障注入恢复旧 EXE。

### 已知边界

- 发布 EXE 未做 Authenticode 商业签名，SmartScreen 可能提醒。
- 天气功能需要访问 Open-Meteo；其他核心数据与工具默认在本机处理。
- 自定义宠物的图片生成由用户自行选择外部工具；WorkMate 不上传照片、不保存第三方 API 密钥。
- 自定义宠物当前使用四张状态图，复杂逐帧表演会回退到轻量程序动效。
