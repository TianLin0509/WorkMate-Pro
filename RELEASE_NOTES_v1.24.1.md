# WorkMate Pro v1.24.1

本版把公司电脑的离线增量更新缩短为：**下载一个差分包 → 拖到桌宠身上 → 确认一次 → 自动重启完成**。

主要变化：

- 桌宠只识别单个 `*.workmate-update.zip`，拖入时显示“松开即可更新”；
- 松开后后台验证 RSA 签名、精确旧版 EXE 哈希、载荷哈希和目标哈希；
- 验证通过只显示一次安装确认，随后复用现有独立更新器、原子替换、健康检查和失败回滚；
- 普通 ZIP、GitHub 源码 ZIP、多个文件及文本不会被误判为更新；
- 能力中心的手动导入改为复用同一协调器，取消安装会清理临时文件，并阻止重复并发验证。

从官方 v1.24.0 升级时，下载同一 Release 中的 `WorkMate-delta-1.24.0-to-1.24.1.workmate-update.zip` 并拖到桌宠身上即可。若当前 EXE 不是官方 v1.24.0 的精确哈希，请使用全量兜底包。

发布 EXE 仍未做 Authenticode 商业签名，SmartScreen 可能在首次运行时提醒。请只从本仓库 Release 下载，并核对 `SHA256SUMS.txt`。
